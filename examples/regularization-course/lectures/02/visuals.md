# Lecture 2 — Visualizations

| # | Slide | Description | TikZ |
|---|-------|-------------|------|
| V01 | 5 | Grouped bar chart: fitted coefficients of two correlated features under ridge / lasso / elastic net, side by side — shows the "grouping effect" | No — needs real data |
| V02 | 8 | Training loss (steadily decreasing) vs. validation loss (decreasing then rising) over epochs, with the early-stopping point marked | No — needs simulated data |
| V03 | 10 | Schematic diagram of a small feedforward network with some hidden units crossed out/greyed to show dropout at one training step | Yes — nodes + arrows, no data needed |
| V04 | 13 | One original image alongside several label-preserving augmented versions (rotated, flipped, shifted, brightness-jittered) | No — real photo (`skimage.data.cat()`, bundled sample image, no network needed) |
