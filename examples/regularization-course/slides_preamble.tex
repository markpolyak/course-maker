% LaTeX/Beamer preamble — pdflatex
% Generated by /course-maker course init. Edit freely.
% Engine-specific packages: fontenc + inputenc (required for pdflatex; remove if switching engines).

\documentclass[aspectratio=169, 10pt]{beamer}

% ── Theme ───────────────────────────────────────────────────────────────────
\usetheme{Madrid}
\usecolortheme{default}
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{footline}[frame number]

% ── Packages ────────────────────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}   % replace with course language: russian, french, german, etc.
% T1 used instead of T2A: this course is English-only and the local TeX
% install lacks texlive-lang-cyrillic (t2aenc.def) with no network access to
% fetch it. If a future lecture needs Cyrillic, switch back to [T2A]{fontenc}
% and install texlive-lang-cyrillic first.
\usepackage{amsmath, amssymb, amsfonts}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric}
\usepackage{xcolor}

% ── Colors ──────────────────────────────────────────────────────────────────
\definecolor{mainblue}{RGB}{46,64,87}
\definecolor{accent}{RGB}{232,72,85}

% ── Title info ──────────────────────────────────────────────────────────────
\title{Regularization in Machine Learning}
\subtitle{Lecture N. [Title]}   % the agent fills: course-language word, lecture number, title
\author{[Author]}
\institute{Claude Cowork}
\date{[Date]}
