# Lecture 01 — Visualizations

| ID | File | Description |
|----|------|-------------|
| V1 | `fig01_sampling.png` | A continuous sine wave with discrete sample points overlaid; samples dense enough to capture the wave. |
| V2 | `fig02_aliasing.png` | A fast sine sampled too slowly, with the spurious low-frequency "alias" it produces drawn through the samples. |

Both are produced by `figures/figures.py` (matplotlib, Agg backend, English labels).
