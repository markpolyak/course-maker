# Profile: polyak — defaults for /course-maker course init dialog.
# Empty string means "no default, ask the user fresh".

# Institution name shown on title slides and in author info.
# TODO: fill in (e.g. "ITMO University")
institution: ""

# Default language for slides, speaker notes, lab materials.
default_language: "Russian"

# LaTeX engine for slide compilation.
default_latex_engine: "xelatex"

# One paragraph: what students know coming in.
# TODO: fill in (varies across courses; leave empty to ask per course)
default_audience: ""

# One paragraph: rigor vs intuition preference, formula handling.
# TODO: fill in (varies across courses)
default_style: ""

# Lab pipeline — used by lab course-init.

# GitHub organisation hosting the starter repos.
# TODO: fill in (likely "markpolyak" personal account or an institutional org)
ghc_org: ""

# Pattern for GitHub Classroom repo names.
# Variables: {year}, {N}, {slug}, {student}.
# {N}-{student} matches GitHub Classroom fork mode where each student's
# accepted assignment lives at <org>/<N>-<student>.
ghc_repo_naming: "{N}-{student}"

# Default instructor name(s) for the title slide / syllabus.
# TODO: fill in
default_instructor: ""
