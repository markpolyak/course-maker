# Profile: local-zip — LMS-related defaults.
#
# This file only contains values that the LMS workflow needs.
# Personal preferences (instructor name, language, latex engine, audience,
# style) belong in ~/.course-maker/defaults.yaml — see profiles/README.md.
#
# local-zip has no LMS-specific defaults; the workflow asks for a delivery
# channel name (free-text) once and uses it only in the publish summary.

# (no fields)
