[pytest]
# tests/ on the import path so test modules can `from conftest import ...`.
pythonpath = tests
testpaths = tests
addopts = -ra
markers =
    e2e: behavioural smoke test that drives the skill via `claude -p` (opt-in, costs tokens)
